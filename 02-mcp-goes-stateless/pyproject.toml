[project]
name = "mcp-goes-stateless"
version = "0.1.0"
description = "runlog-memory server (same as video 1) plus protocol_diff.py, the live LEGACY vs MODERN negotiation proof from RUNLOG video 2."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=2.0.0",
]
